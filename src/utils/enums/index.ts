export enum EnumGroupBy {
  NONE = "none",
  FAMILY = "family",
  ORDER = "order",
  GENUS = "genus",
}

export enum EnumViewType {
  LIST = "list",
  TABLE = "table",
}

export enum EnumFruitJarDispatchType {
  ADD = "add",
}
